generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Todo {
  id          Int       @id @default(autoincrement())
  text        String    @unique
  completedAt DateTime? @db.Timestamp()
}
